import 'package:flutter/material.dart';

class SubtitleText extends StatelessWidget {
  const SubtitleText({super.key});

  @override
  Widget build(context) {
    return Text(
      '40. Creating Custom Widgets',
      style: TextStyle(fontSize: 16, color: Colors.black),
    );
  }
}
